# Optional: Vercel supplies the deployment URL automatically.
# For an approved custom-domain production build, set SITE_URL in Vercel.
# SITE_URL=https://www.unitalfa.in

# Leave false during client review. Set true in Production only after approval.
# Preview deployments always remain noindex, even if they inherit this flag.
SITE_INDEXABLE=false
